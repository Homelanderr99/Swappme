# Verificación de conexión con GitHub

Este archivo valida la conexión y flujo git solicitado.

## Resultados

- Remoto configurado:
  ```
  origin  https://github.com/Homelanderr99/Swappme.git (fetch)
  origin  https://github.com/Homelanderr99/Swappme.git (push)
  ```
- Rama creada: `feature/nueva-funcionalidad`
- Helper de credenciales: `osxkeychain`
- GitHub CLI: instalado (`gh 2.82.1`), autenticación pendiente

## Próximos pasos (si hace falta autenticación)

- Ejecutar `gh auth login` y seguir el flujo de navegador
- Alternativa: generar un PAT y dejar que el llavero de macOS lo recuerde al primer push

## Notas

Este archivo no modifica la estructura del proyecto y solo sirve para verificación.